{
  "name": "ai-in-practice-ty-portal",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/database": "latest",
    "@netlify/functions": "latest"
  }
}
