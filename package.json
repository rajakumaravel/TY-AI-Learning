{
  "name": "ai-in-practice-ty-portal",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@netlify/database": "latest",
    "@netlify/functions": "latest",
    "@netlify/identity": "latest"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
