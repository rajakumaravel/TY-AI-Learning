{
  "name": "ai-in-practice-ty-portal",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build",
    "acceptance": "node tests/acceptance/cutover-acceptance.mjs && node tests/acceptance/ui-acceptance.mjs",
    "datasets": "node scripts/generate-datasets.mjs"
  },
  "dependencies": {
    "@netlify/database": "latest",
    "@netlify/functions": "latest",
    "@netlify/identity": "latest",
    "@supabase/supabase-js": "latest"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "latest"
  }
}
