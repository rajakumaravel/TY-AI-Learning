import './project-workspace.css';
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const safeHref=(v='')=>{try{const u=new URL(String(v).trim());return u.protocol==='http:'||u.protocol==='https:'?u.href:''}catch{return ''}};
let selectedStudent=null;
async function req(path,opts={}){const r=await fetch('/api/projects/'+path,{credentials:'same-origin',headers:{'content-type':'application/json'},...opts});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d}
function renderProject(p){const snapshot=p.submittedSnapshot||p.workspace||{};const logs=Array.isArray(snapshot.workLog)?snapshot.workLog:[];const evidence=Array.isArray(snapshot.evidence)?snapshot.evidence:[];return `<article class="assessment-review-card" data-project="${esc(p.projectId)}"><div class="assessment-review-head"><strong>${esc(p.brief?.title||p.projectId)}</strong><span>${esc(p.status)}</span></div><p><strong>Role:</strong> ${esc(p.brief?.role||'')} · <strong>Client:</strong> ${esc(p.brief?.client||'')}</p><p><strong>Submitted:</strong> ${esc(p.submittedAt||'Not submitted')}</p><h4>Work log</h4>${logs.length?logs.map((x,i)=>`<div class="pw-admin-evidence"><strong>Entry ${i+1}</strong><p><b>Did:</b> ${esc(x.did||'')}</p><p><b>Result:</b> ${esc(x.result||'')}</p><p><b>Blocker:</b> ${esc(x.blocker||'—')}</p><p><b>Decision:</b> ${esc(x.decision||'—')}</p><p><b>Next:</b> ${esc(x.next||'—')}</p></div>`).join(''):'<p class="muted">No work-log entries.</p>'}<h4>Submitted evidence</h4>${evidence.length?evidence.map(x=>`<div class="pw-admin-evidence"><strong>${esc(x.label||'Evidence')}</strong><p>${esc(x.note||'')}</p>${safeHref(x.url)?`<a href="${esc(safeHref(x.url))}" target="_blank" rel="noopener noreferrer">Open evidence link ↗</a>`:(x.url?`<p class="muted">Link not shown (only http/https links are allowed): ${esc(x.url)}</p>`:'')}</div>`).join(''):'<p class="muted">No evidence recorded.</p>'}<h4>Final recommendation</h4><p>${esc(snapshot.finalRecommendation||'Not submitted')}</p>${p.submittedSnapshot?`<label>Teacher/admin comment<textarea data-review-comment>${esc(p.reviewComment||'')}</textarea></label><button class="secondary compact" data-review-project>Mark reviewed</button><span class="review-status">${p.reviewedAt?'Reviewed':'Awaiting review'}</span>`:''}</article>`}
function render(projects=[]){let host=document.getElementById('projectReviewPanel');if(!host){host=document.createElement('section');host.id='projectReviewPanel';host.className='assessment-review-panel';document.getElementById('studentDetail')?.appendChild(host)}host.innerHTML=`<div class="eyebrow">PROJECT WORKSPACE</div><h3>Submitted project evidence</h3><p class="muted">The submitted snapshot is shown so later student edits do not change what was reviewed.</p>${projects.length?projects.map(renderProject).join(''):'<p class="muted">No project workspace yet.</p>'}`;host.querySelectorAll('[data-review-project]').forEach(btn=>btn.onclick=async()=>{const card=btn.closest('[data-project]');btn.disabled=true;try{await req(`admin/student/${encodeURIComponent(selectedStudent)}/${encodeURIComponent(card.dataset.project)}/review`,{method:'PUT',body:JSON.stringify({comment:card.querySelector('[data-review-comment]').value})});card.querySelector('.review-status').textContent='Reviewed · saved'}catch(e){card.querySelector('.review-status').textContent=e.message}finally{btn.disabled=false}})}
async function load(id){selectedStudent=id;try{const d=await req(`admin/student/${encodeURIComponent(id)}`);render(d.projects||[])}catch{}}
document.addEventListener('click',e=>{const b=e.target.closest('[data-student]');if(b)setTimeout(()=>load(b.dataset.student),300)});
